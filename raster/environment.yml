# NOTE: apt-get install build-essential contains required compilers on debian
---
name: rasterenv
channels:
        - conda-forge

dependencies:
        - python=3.6
        - gdal>=2.2.3
        - rasterio>=1.0
        - cython
        - jupyter
        - matplotlib=2.2.3
        - numpy
        - pyproj
        - shapely
        - pip:
            - pygeotools
            - pygeoprocessing
